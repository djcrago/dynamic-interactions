import displaySlide from "./displaySlide";
import colorIndicator from "./colorIndicator";

export default function displaySlideController(index) {
    displaySlide(index);

    colorIndicator(index);

}
